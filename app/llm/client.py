"""
LLM клиент для работы с Qwen через Ollama
Версия 2.0 — с ASO методологией, фризами и iOS обновлениями
"""

import json
import requests
from typing import Optional, List, Dict, Any
from pathlib import Path
import pandas as pd
import warnings
warnings.filterwarnings('ignore')


class LLMClient:
    """Клиент для работы с LLM (Qwen через Ollama) с ASO методологией"""
    
    # =====================================================
    # ASO МЕТОДОЛОГИЯ (расширенная версия 2.0)
    # =====================================================
    ASO_METHODOLOGY = """
    # ASO МЕТОДОЛОГИЯ ДЛЯ SOUND AMPLIFIER v2.0
    
    ## 1. КЛЮЧЕВЫЕ ПОНЯТИЯ
    - **Мотив** — показатель активности продвижения ключевого слова
    - **Позиция** — место ключевого слова в поисковой выдаче (1 = лучшее)
    - **Органика** — органический трафик из App Store
    - **Активации** — платные установки
    - **Эффективность мотива** — сколько позиций даёт 1 единица мотива
    
    ## 2. СЦЕНАРИИ РАБОТЫ С МОТИВОМ
    - **Резкий рост (+30%)** — агрессивное продвижение
    - **Плавный рост** — постепенное увеличение
    - **Стабильный** — поддержание текущего уровня
    - **Резкий спад (-30%)** — снижение активности
    - **Плавный спад** — постепенное снижение
    
    ## 3. КЛАСТЕРЫ КЛЮЧЕВЫХ СЛОВ
    - **top10_high** — высокий мотив, топ-10 позиция
    - **top10_medium** — средний мотив, топ-10 позиция
    - **top50_high** — высокий мотив, топ-50 позиция
    - **above50_low** — низкий мотив, ниже 50 позиции
    
    ## 4. ПРАВИЛА ПРОДВИЖЕНИЯ (R1-R14)
    - R1: Базовый цикл: M = 1 → 1 → 0
    - R2: Оценка динамики после 2-3 дней
    - R3: Увеличение мотива при росте > 10 позиций
    - R4: Пропуск при падении позиции
    - R5: Ограничение мотива до 2 для новых запросов
    - R6: Целевые позиции по частотности
    - R7: Максимум 3 цикла с мотивом 2
    - R8: Повышение мотива до 3 при росте 50%
    - R9: Процентный шаг (15%/10%)
    - R10: Откат -30% при отсутствии роста
    - R11: Поддержка снижением на 2-3 единицы
    - R12: Максимальный мотив 30% от трафика
    - R13: Пауза во время фризов
    - R14: Усиление после iOS обновлений
    
    ## 5. ФРИЗЫ И ОБНОВЛЕНИЯ iOS
    
    ### Периоды фризов (когда продвижение не работает)
    - 04.01.2025 – 08.01.2025 (Новогодний)
    - 25.02.2025 – 10.03.2025 (Февральский)
    - 15.03.2025 (короткий)
    - 05.05.2025 – 12.05.2025 (Майский)
    - 15.08.2025 – 30.08.2025 (Августовский)
    - 10.09.2025 – 16.09.2025 (Сентябрьский)
    - 28.09.2025 – 31.10.2025 (Осенний)
    - 06.01.2026 – 19.01.2026 (Новогодний 2026)
    - 07.04.2026 – 21.04.2026 (Апрельский)
    - 29.06.2026 – 18.07.2026 (Летний)
    
    **Во время фризов:**
    - Продвижение не даёт эффекта
    - Рекомендуется сделать паузу
    - Мотив не влияет на позиции
    
    ### iOS обновления (периоды активности)
    - 31.03.2025 – 18.4
    - 12.05.2025 – 18.5
    - 29.07.2025 – 18.6
    - 15.09.2025 – 18.7 и 26.0
    - 03.11.2025 – 26.1
    - 12.12.2025 – 26.2
    - 11.02.2026 – 26.3
    - 24.03.2026 – 26.4
    - 31.03.2026 – 26.4.1
    - 11.05.2026 – 26.5
    - 01.06.2026 – 26.5.1
    - 29.06.2026 – 26.5.2
    - 27.07.2026 – 26.6
    - 17.08.2026 – 26.6.1
    
    **Во время iOS обновлений:**
    - Неделя после обновления — пик активности пользователей
    - Возможны изменения в алгоритмах поиска
    - Рекомендуется усилить мониторинг
    
    ## 6. АНАЛИЗ ДАННЫХ
    - Используйте MAE, RMSE, R² для оценки моделей
    - XGBoost с регуляризацией — лучшая модель
    - Прогноз на 1 день (position_delta_1d) даёт точные краткосрочные оценки
    - Важнейшие признаки: position, position_ma_14, position_ma_7, is_freeze, is_ios_update_day
    - Мотив важен, но не является главным фактором (8-10%)
    
    ## 7. ЭФФЕКТИВНОСТЬ МОТИВА
    **Формула:** efficiency = (avg_pos_without - avg_pos_with) / avg_motiv
    
    **Интерпретация:**
    - Эффективность > 5: отличная (1 ед. мотива = 5+ позиций)
    - Эффективность 1-5: хорошая
    - Эффективность 0.3-1: средняя
    - Эффективность < 0.3: низкая
    - Эффективность ≤ 0: не работает
    
    ## 8. РАСЧЁТ ТРЕБУЕМОГО МОТИВА
    **Формула:** required_motiv = (current_position - target_position) / efficiency
    
    **Рекомендации по величине мотива:**
    - 1-2: небольшое увеличение (R5)
    - 3-5: умеренное увеличение (R9)
    - 6-10: значительное увеличение (R8)
    - > 10: агрессивное продвижение (R12)
    """
    
    def __init__(self, base_url: str = "http://localhost:11434", model: str = "qwen2.5:14b"):
        self.base_url = base_url
        self.model = model
        self.is_available = False
        self._check_availability()
    
    def _check_availability(self):
        """Проверка доступности Ollama"""
        try:
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            if response.status_code == 200:
                models = response.json().get('models', [])
                model_names = [m.get('name', '') for m in models]
                if any(self.model in name for name in model_names):
                    self.is_available = True
                    print(f"✅ LLM клиент готов (модель: {self.model})")
                else:
                    print(f"⚠️ Модель {self.model} не найдена. Доступны: {model_names}")
            else:
                print(f"⚠️ Не удалось подключиться к Ollama (статус: {response.status_code})")
        except Exception as e:
            print(f"⚠️ Ошибка подключения к Ollama: {e}")
    
    def _get_system_prompt(self, role: str = "analyst") -> str:
        """Получить системный промпт с методологией"""
        prompts = {
            "analyst": f"""
Ты — эксперт по ASO (App Store Optimization) для приложения Sound Amplifier.

{self.ASO_METHODOLOGY}

Твоя задача:
1. Анализировать данные ASO и давать профессиональные интерпретации
2. Отвечать на вопросы по методологии и данным
3. Давать рекомендации по продвижению ключевых слов
4. Учитывать фризы и iOS обновления при планировании
5. Объяснять кластеры и сценарии работы с мотивом

Всегда используй русский язык. Отвечай структурированно, с чёткими выводами.
Если не знаешь ответа — честно говори об этом.
""",
            "plots": f"""
Ты — специалист по ASO визуализации данных.

{self.ASO_METHODOLOGY}

Твоя задача:
1. Объяснять графики и визуализации
2. Интерпретировать тренды и паттерны
3. Давать рекомендации на основе графиков
4. Связывать разные графики в единую картину
5. Учитывать влияние фризов и iOS обновлений на графиках

Всегда используй русский язык. Отвечай структурированно.
""",
            "recommendations": f"""
Ты — ASO-консультант для приложения Sound Amplifier.

{self.ASO_METHODOLOGY}

Твоя задача:
1. Давать конкретные рекомендации по продвижению
2. Определять приоритетные ключевые слова
3. Предлагать оптимальные сценарии работы с мотивом
4. Оценивать риски и возможности
5. Учитывать календарь фризов и iOS обновлений

Всегда используй русский язык. Рекомендации должны быть конкретными и измеримыми.
"""
        }
        return prompts.get(role, prompts["analyst"])
    
    def _generate(self, prompt: str, role: str = "analyst") -> str:
        """Генерация ответа от LLM с методологией"""
        if not self.is_available:
            return "⚠️ LLM недоступен. Проверьте подключение к Ollama.\n\n" + self.ASO_METHODOLOGY
        
        messages = [
            {"role": "system", "content": self._get_system_prompt(role)},
            {"role": "user", "content": prompt}
        ]
        
        try:
            response = requests.post(
                f"{self.base_url}/api/chat",
                json={
                    "model": self.model,
                    "messages": messages,
                    "stream": False,
                    "options": {
                        "temperature": 0.7,
                        "top_p": 0.9,
                        "max_tokens": 4000
                    }
                },
                timeout=180
            )
            
            if response.status_code == 200:
                result = response.json()
                return result.get('message', {}).get('content', 'Нет ответа')
            else:
                return f"⚠️ Ошибка: {response.status_code}"
        except Exception as e:
            return f"⚠️ Ошибка запроса: {e}"
    
    # =====================================================
    # БАЗОВЫЕ МЕТОДЫ АНАЛИЗА
    # =====================================================
    
    def analyze_data(self, data_summary: Dict[str, Any]) -> str:
        """Анализ данных с применением методологии"""
        prompt = f"""
Проанализируй данные ASO для приложения Sound Amplifier.

📊 ДАННЫЕ:
- Всего записей: {data_summary.get('total_records', 'N/A')}
- Уникальных ключей: {data_summary.get('unique_keywords', 'N/A')}
- Количество кластеров: {data_summary.get('unique_clusters', 'N/A')}
- Период: {data_summary.get('period_start', 'N/A')} - {data_summary.get('period_end', 'N/A')}
- Средний мотив: {data_summary.get('avg_motiv', 'N/A')}
- Средняя позиция: {data_summary.get('avg_position', 'N/A')}

Вопросы для анализа:
1. Какие ключевые выводы можно сделать из этих данных?
2. Есть ли аномалии или необычные паттерны?
3. Какие ключевые слова выглядят наиболее перспективными?
4. Какие риски вы видите в этих данных?
5. Как влияют фризы и iOS обновления на общую картину?

Используй ASO методологию для ответа.
"""
        return self._generate(prompt, "analyst")
    
    def generate_hypotheses(self, data_summary: Dict[str, Any]) -> str:
        """Генерация гипотез на основе данных"""
        prompt = f"""
На основе данных ASO сгенерируй гипотезы для тестирования.

📊 ДАННЫЕ:
- Всего записей: {data_summary.get('total_records', 'N/A')}
- Уникальных ключей: {data_summary.get('unique_keywords', 'N/A')}
- Количество кластеров: {data_summary.get('unique_clusters', 'N/A')}
- Период: {data_summary.get('period_start', 'N/A')} - {data_summary.get('period_end', 'N/A')}

Сформулируй 5-7 гипотез в формате:
H1: [Название]
   - Описание: ...
   - Ожидаемый результат: ...
   - Метод проверки: ...
   - Связь с правилами методологии: ...

Используй правила из методологии (R1-R14) при формулировке гипотез.
Особое внимание удели влиянию фризов и iOS обновлений.
"""
        return self._generate(prompt, "analyst")
    
    def generate_strategy(self, cluster_info: Dict[str, Any]) -> str:
        """Генерация стратегии продвижения по кластерам"""
        prompt = f"""
Разработай стратегию ASO продвижения по кластерам.

📊 КЛАСТЕРЫ:
{json.dumps(cluster_info, indent=2, ensure_ascii=False)[:5000]}

На основе данных предложи:
1. Стратегию для каждого кластера с учётом правил R1-R14
2. Приоритетные кластеры для продвижения
3. Рекомендации по распределению мотива
4. Учёт фризов при планировании
5. Ожидаемые результаты

Формат ответа:
## Стратегия по кластерам
### [Кластер 1]
- Текущее состояние: ...
- Рекомендуемый сценарий: ...
- Действия: ...

## Приоритеты
...

## Учёт фризов и iOS обновлений
...

## Ожидаемые результаты
...
"""
        return self._generate(prompt, "recommendations")
    
    def explain_clusters(self, cluster_stats: pd.DataFrame) -> str:
        """Объяснение кластеров с визуализацией методологии"""
        cluster_text = cluster_stats.to_string()
        prompt = f"""
Объясни результаты кластеризации ключевых слов.

📊 СТАТИСТИКА ПО КЛАСТЕРАМ:
{cluster_text}

Вопросы:
1. Что означают эти кластеры с бизнес-точки зрения?
2. Как они соотносятся с правилами методологии (R1-R14)?
3. Какие кластеры наиболее ценные?
4. Какие кластеры требуют немедленного внимания?
5. Как использовать эту информацию для ASO-стратегии?

Дай практические рекомендации по работе с каждым кластером.
"""
        return self._generate(prompt, "analyst")
    
    def explain_plot(self, plot_name: str, plot_description: str, data_context: str = "") -> str:
        """Объяснение графика с методологией"""
        prompt = f"""
Объясни график ASO-аналитики.

📈 ИНФОРМАЦИЯ О ГРАФИКЕ:
- Название: {plot_name}
- Описание: {plot_description}
- Контекст данных: {data_context if data_context else "Не указан"}

Вопросы:
1. Что показывает этот график?
2. Какие тренды или паттерны видны?
3. Как это связано с методологией ASO (кластеры, сценарии, правила)?
4. Видны ли на графике периоды фризов или iOS обновлений?
5. Какие выводы можно сделать для стратегии продвижения?
6. Какие следующие шаги рекомендуете?

Используй профессиональную ASO терминологию.
"""
        return self._generate(prompt, "plots")
    
    def chat(self, question: str, context: Optional[str] = None) -> str:
        """Свободный диалог с методологией"""
        context_prompt = f"""
Контекст: {context if context else "Общий ASO анализ"}

Вопрос: {question}

Используй ASO методологию для ответа. Если вопрос о графиках — дай детальную интерпретацию.
Если вопрос о стратегии — дай конкретные рекомендации с учётом правил R1-R14.
Если вопрос о фризах или iOS обновлениях — учитывай календарь событий.
"""
        return self._generate(context_prompt, "analyst")
    
    def get_model_explanation(self, model_results: Dict[str, Any]) -> str:
        """Объяснение результатов ML модели"""
        prompt = f"""
Объясни результаты ML модели для прогнозирования изменения позиции на 1 день.

📊 РЕЗУЛЬТАТЫ МОДЕЛИ:
{json.dumps(model_results, indent=2, ensure_ascii=False)[:5000]}

Вопросы:
1. Что означают метрики MAE, RMSE, R²?
2. Какие признаки наиболее важны и почему?
3. Как это согласуется с ASO методологией?
4. Какую роль играют признаки фризов и iOS обновлений?
5. Какие практические выводы можно сделать?
6. Как улучшить модель?

Дай интерпретацию для бизнес-пользователя.
"""
        return self._generate(prompt, "analyst")
    
    def get_recommendations(self, data_summary: Dict[str, Any]) -> str:
        """Получение рекомендаций на основе всех данных"""
        prompt = f"""
Дай конкретные ASO-рекомендации на основе данных.

📊 ДАННЫЕ:
{json.dumps(data_summary, indent=2, ensure_ascii=False)}

Сформулируй рекомендации по категориям:
1. 🔴 КРИТИЧЕСКИЕ (срочные действия)
2. 🟡 ВАЖНЫЕ (ближайшие действия)
3. 🟢 РЕКОМЕНДУЕМЫЕ (долгосрочные действия)

Для каждой рекомендации укажи:
- Что делать
- Почему это важно (связь с методологией)
- Ожидаемый эффект
- Сроки

Используй правила R1-R14 для обоснования.
Учитывай календарь фризов и iOS обновлений при планировании.
"""
        return self._generate(prompt, "recommendations")
    
    # =====================================================
    # НОВЫЕ МЕТОДЫ: РАСЧЁТ МОТИВА И ЭФФЕКТИВНОСТЬ
    # =====================================================
    
    def explain_motiv_calculation(self, keyword: str, motiv_data: dict) -> str:
        """Объяснение расчёта требуемого мотива"""
        prompt = f"""
Объясни расчёт требуемого мотива для ключевого слова.

🎯 КЛЮЧЕВОЕ СЛОВО: {keyword}

📊 РАСЧЁТ:
- Текущая позиция: {motiv_data.get('current_position', 'N/A')}
- Целевая позиция: {motiv_data.get('target_position', 'N/A')}
- Текущий мотив: {motiv_data.get('current_motiv', 'N/A')}
- Требуемый мотив: {motiv_data.get('required_motiv', 'N/A')}
- Эффективность мотива: {motiv_data.get('efficiency', 'N/A')}
- Прогноз через 1 день: {motiv_data.get('predicted_position_1d', 'N/A')}
- Прогноз через 7 дней: {motiv_data.get('predicted_position_7d', 'N/A')}
- Уверенность: {motiv_data.get('confidence', 'N/A')}

Вопросы:
1. Как интерпретировать этот расчёт?
2. Какие риски при достижении цели?
3. Какой сценарий продвижения использовать (R5, R8, R9, R12)?
4. Нужно ли учитывать фризы при планировании?
5. Какие альтернативные стратегии возможны?

Дай практические рекомендации.
"""
        return self._generate(prompt, "recommendations")
    
    def explain_motiv_efficiency(self, keyword: str, efficiency_data: dict) -> str:
        """Объяснение эффективности мотива"""
        prompt = f"""
Объясни эффективность мотива для ключевого слова.

🎯 КЛЮЧЕВОЕ СЛОВО: {keyword}

📊 ЭФФЕКТИВНОСТЬ:
- Средняя позиция с мотивом: {efficiency_data.get('avg_pos_with_motiv', 'N/A')}
- Средняя позиция без мотива: {efficiency_data.get('avg_pos_without_motiv', 'N/A')}
- Средний мотив: {efficiency_data.get('avg_motiv', 'N/A')}
- Эффективность: {efficiency_data.get('efficiency', 'N/A')}
- Корреляция мотива и позиции: {efficiency_data.get('correlation', 'N/A')}
- Лучшая позиция: {efficiency_data.get('best_position', 'N/A')}
- Лучший мотив: {efficiency_data.get('best_motiv', 'N/A')}

Вопросы:
1. Как интерпретировать эту эффективность?
2. Что означает корреляция?
3. Как это использовать для планирования?
4. Есть ли аномалии в данных?
5. Какие рекомендации по работе с этим ключом?

Используй формулу эффективности из методологии.
"""
        return self._generate(prompt, "analyst")
    
    def analyze_efficiency_batch(self, top_efficient: List[dict], worst_efficient: List[dict]) -> str:
        """Анализ эффективности по всем ключам"""
        prompt = f"""
Проанализируй эффективность мотива по всем ключам.

🏆 ТОП-10 САМЫХ ЭФФЕКТИВНЫХ КЛЮЧЕЙ:
{json.dumps(top_efficient, indent=2, ensure_ascii=False)}

⚠️ ТОП-10 СЛАБЫХ КЛЮЧЕЙ:
{json.dumps(worst_efficient, indent=2, ensure_ascii=False)}

Вопросы:
1. Какие паттерны видны в самых эффективных ключах?
2. Что общего у слабых ключей?
3. Как использовать эту информацию для ASO?
4. Какие ключи требуют особого подхода?
5. Какие стратегии продвижения применить для каждой группы?

Дай практические рекомендации.
"""
        return self._generate(prompt, "analyst")
    
    # =====================================================
    # НОВЫЕ МЕТОДЫ: ПРОВЕРКА ГИПОТЕЗ ПО КЛЮЧУ
    # =====================================================
    
    def check_keyword_hypothesis(self, keyword: str, keyword_data: dict, target_position: int = None) -> str:
        """Проверка гипотез для конкретного ключевого слова"""
        
        context = f"""
КЛЮЧЕВОЕ СЛОВО: {keyword}

ДАННЫЕ:
- Всего записей: {keyword_data.get('n_records', 'N/A')}
- Текущая позиция: {keyword_data.get('current_position', 'N/A')}
- Средняя позиция: {keyword_data.get('avg_position', 'N/A')}
- Минимальная позиция: {keyword_data.get('min_position', 'N/A')}
- Максимальная позиция: {keyword_data.get('max_position', 'N/A')}
- Текущий мотив: {keyword_data.get('current_motiv', 'N/A')}
- Средний мотив: {keyword_data.get('avg_motiv', 'N/A')}
- Максимальный мотив: {keyword_data.get('max_motiv', 'N/A')}
- Средняя органика: {keyword_data.get('organic_avg', 'N/A')}
- Средние активации: {keyword_data.get('activation_avg', 'N/A')}
- Период: {keyword_data.get('period_start', 'N/A')} - {keyword_data.get('period_end', 'N/A')}
"""
        
        if target_position:
            context += f"\nЦЕЛЕВАЯ ПОЗИЦИЯ: {target_position}\n"
        
        prompt = f"""
Проведи проверку гипотез для ключевого слова и предложи новые гипотезы.

{context}

На основе ASO методологии:

1. ПРОВЕРЬ СУЩЕСТВУЮЩИЕ ГИПОТЕЗЫ:
   - H1: Похожие запросы (корреляция позиций)
   - H3: Эффективность сценариев мотива
   - H4: Влияние фризов (мотив = 0)
   - H5: Влияние мотива на позицию
   - H7: Влияние фризов (события)
   - H8: Влияние iOS обновлений
   
2. ОПРЕДЕЛИ:
   - Какие гипотезы подтверждаются для этого ключа?
   - Какие закономерности видны в данных?
   
3. ПРЕДЛОЖИ 3-5 НОВЫХ ГИПОТЕЗ:
   - На основе паттернов в данных
   - С учётом правил R1-R14
   - С конкретными метриками проверки
   
Формат ответа:
## Проверка существующих гипотез
### H1: [Название]
- Результат: ✅ ПОДТВЕРЖДЕНА / ❌ НЕ ПОДТВЕРЖДЕНА / ⚠️ НЕТ ДАННЫХ
- Обоснование: ...

## Новые гипотезы
### H_New_1: [Название]
- Гипотеза: ...
- Метрика: ...
- Правило: R...
- Метод проверки: ...

## Рекомендации
- ...
"""
        return self._generate(prompt, "analyst")
    
    def generate_new_hypotheses(self, data_context: dict) -> str:
        """Генерация новых гипотез на основе данных"""
        
        prompt = f"""
На основе анализа данных ASO сгенерируй новые гипотезы для тестирования.

ДАННЫЕ:
- Всего ключей: {data_context.get('total_keywords', 'N/A')}
- Всего записей: {data_context.get('total_records', 'N/A')}
- Период: {data_context.get('period', 'N/A')}
- Средняя позиция: {data_context.get('avg_position', 'N/A')}
- Средний мотив: {data_context.get('avg_motiv', 'N/A')}

РЕЗУЛЬТАТЫ ТЕСТОВ:
{data_context.get('hypotheses_results', 'N/A')[:2000]}

Сгенерируй 5-7 новых гипотез в формате:
H_New_1: [Название]
   - Гипотеза: [чёткая формулировка]
   - Метрика: [как измерять]
   - Правило методологии: [R1-R14]
   - Ожидаемый результат: [что должно произойти]
   - Метод проверки: [статистический тест]

Используй ASO методологию и правила продвижения.
Особое внимание удели неисследованным областям (фризы, iOS, сезонность).
"""
        return self._generate(prompt, "analyst")
    
    # =====================================================
    # НОВЫЕ МЕТОДЫ: АНАЛИЗ ФРИЗОВ И iOS ОБНОВЛЕНИЙ
    # =====================================================
    
    def analyze_freeze_impact(self, freeze_data: dict) -> str:
        """Анализ влияния фриза"""
        
        prompt = f"""
Проанализируй влияние фриза на продвижение.

ДАННЫЕ ФРИЗА:
{json.dumps(freeze_data, indent=2, ensure_ascii=False, default=str)[:5000]}

Ответь на вопросы:
1. Как фриз повлиял на позиции ключей?
2. Была ли пауза в продвижении (снижение мотива)?
3. Какие ключи пострадали больше всего?
4. Сравни данные до, во время и после фриза
5. Какие рекомендации на будущее?

Формат ответа:
## Анализ влияния фриза
### До фриза
- ...

### Во время фриза
- ...

### После фриза
- ...

## Ключевые выводы
- ...

## Рекомендации
- ...
"""
        return self._generate(prompt, "analyst")
    
    def analyze_ios_update_impact(self, update_data: dict) -> str:
        """Анализ влияния iOS обновления"""
        
        prompt = f"""
Проанализируй влияние iOS обновления на продвижение.

ДАННЫЕ ОБНОВЛЕНИЯ:
{json.dumps(update_data, indent=2, ensure_ascii=False, default=str)[:5000]}

Ответь на вопросы:
1. Как обновление повлияло на позиции?
2. Был ли рост активности пользователей?
3. Стоит ли усиливать продвижение в такие периоды?
4. Сравни данные до обновления, первую и вторую неделю после
5. Какие рекомендации на будущее?

Формат ответа:
## Анализ влияния iOS обновления
### До обновления
- ...

### Первая неделя после
- ...

### Вторая неделя после
- ...

## Ключевые выводы
- ...

## Рекомендации
- ...
"""
        return self._generate(prompt, "analyst")
    
    def analyze_freeze_calendar(self, next_freeze: dict) -> str:
        """Анализ календаря фризов и рекомендации"""
        
        prompt = f"""
Проанализируй следующий фриз и дай рекомендации по подготовке.

СЛЕДУЮЩИЙ ФРИЗ:
- Название: {next_freeze.get('name', 'N/A')}
- Начало: {next_freeze.get('start', 'N/A')}
- Конец: {next_freeze.get('end', 'N/A')}
- Дней до начала: {next_freeze.get('days_until', 'N/A')}

Вопросы:
1. Как подготовиться к фризу?
2. Когда нужно приостановить продвижение?
3. Что делать во время фриза?
4. Как возобновить продвижение после фриза?
5. Какие ключи подготовить для усиления после?

Дай практический план действий с датами.
"""
        return self._generate(prompt, "recommendations")
    
    # =====================================================
    # ВСПОМОГАТЕЛЬНЫЕ МЕТОДЫ
    # =====================================================
    
    def get_status(self) -> dict:
        """Получение статуса LLM"""
        return {
            'is_available': self.is_available,
            'model': self.model,
            'url': self.base_url,
            'methodology_version': '2.0'
        }
    
    def test_connection(self) -> bool:
        """Тест подключения к LLM"""
        if not self.is_available:
            return False
        
        try:
            response = self._generate("Ответь одним словом: OK", "analyst")
            return "OK" in response or "ok" in response.lower()
        except:
            return False
    
    def get_methodology(self) -> str:
        """Получить текст методологии"""
        return self.ASO_METHODOLOGY
    
    def reload_methodology(self):
        """Перезагрузить методологию (если нужно обновить)"""
        # Методология уже в классе, этот метод для будущих обновлений
        print("✅ Методология актуальна (версия 2.0)")
        return True